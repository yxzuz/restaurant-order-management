<template>
  <div v-show="isActive" role="tabpanel" :class="classes">
    <slot />
  </div>
</template>

<script setup>
import { computed, inject } from 'vue'

import { cn } from '@/lib/utils'

const props = defineProps({
  value: {
    type: String,
    required: true,
  },
  class: {
    type: String,
    default: '',
  },
})

const tabsValue = inject('tabsValue')
const isActive = computed(() => tabsValue?.value === props.value)
const classes = computed(() => cn('mt-2 ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2', props.class))
</script>
